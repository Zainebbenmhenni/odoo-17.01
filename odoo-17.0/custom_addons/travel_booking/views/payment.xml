<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="payment_options_template" name="Options de Paiement">
    <t t-call="website.layout">
        <div class="container mt-4">
            <div class="row">
                <div class="col-12">
                    <h2>Options de Paiement</h2>
                    <div class="card mb-4">
                        <div class="card-body">
                            <h5 class="card-title">Récapitulatif de votre réservation</h5>
                            <div class="row">
                                <div class="col-md-3">
                                    <strong>Compagnie:</strong> <span t-esc="booking.airline"/>
                                </div>
                                <div class="col-md-3">
                                    <strong>Vol:</strong> <span t-esc="booking.flight_number"/>
                                </div>
                                <div class="col-md-3">
                                    <strong>Prix:</strong> <span t-esc="booking.price"/> <t t-esc="currency"/>
                                </div>
                            </div>
                            <div class="row mt-3">
                                <div class="col-md-6">
                                    <strong>Départ:</strong> <span t-esc="booking.departure_date"/> à <span t-esc="booking.departure_time"/>
                                </div>
                                <div class="col-md-6">
                                    <strong>Arrivée:</strong> <span t-esc="booking.arrival_date"/> à <span t-esc="booking.arrival_time"/>
                                </div>
                            </div>
                            <div class="row mt-3">
                                <div class="col-md-6">
                                    <strong>Passager:</strong> <span t-esc="booking.name"/>
                                </div>
                                <div class="col-md-6">
                                    <strong>Email:</strong> <span t-esc="booking.email"/>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="card">
                        <div class="card-header">
                            <h5>Choisissez votre méthode de paiement</h5>
                        </div>
                        <div class="card-body">
                            <form t-attf-action="/travel/payment/process" method="post" class="o_website_form">
                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                <input type="hidden" name="booking_id" t-att-value="booking.id"/>
                                <script src="https://js.stripe.com/v3/"></script>
                                <div class="form-group">
                                    <label for="card-element">Carte de crédit ou de débit</label>
                                    <div id="card-element" class="form-control">
                                        <!-- Un élément Stripe sera inséré ici. -->
                                    </div>
                                    <div id="card-errors" role="alert"></div>
                                </div>
                                <button type="submit" class="btn btn-primary">Payer</button>
                            </form>
                            <script>
                                var stripe = Stripe(
                                'pk_test_51R0hP2R3N0dhVd1wymFE1CNjvQlsAmxZzFsSWsWFijTE79nhNvP0HrKMPPmlDTqWuERRbXMt1x7x6tbCHDPuucGF00Uoa4Ovor');
                                var elements = stripe.elements();
                                var card = elements.create('card');
                                card.mount('#card-element');
                                card.on('change', function(event) {
                                var displayError = document.getElementById('card-errors');
                                if (event.error) {
                                displayError.textContent = event.error.message;
                                } else {
                                displayError.textContent = '';
                                }
                                });

                                var form = document.querySelector('.o_website_form');
                                form.addEventListener('submit', function(event) {
                                event.preventDefault();
                                stripe.createToken(card).then(function(result) {
                                if (result.error) {
                                var errorElement = document.getElementById('card-errors');
                                errorElement.textContent = result.error.message;
                                } else {
                                var hiddenInput = document.createElement('input');
                                hiddenInput.setAttribute('type', 'hidden');
                                hiddenInput.setAttribute('name', 'stripeToken');
                                hiddenInput.setAttribute('value', result.token.id);
                                form.appendChild(hiddenInput);
                                form.submit();
                                } });
                                });
                            </script>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </t>
</template>
</odoo>
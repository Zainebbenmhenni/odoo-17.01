<odoo>
    <template id="welcome_page" name="Welcome Page">
        <t t-call="website.layout">
            <style>
                /* Palette de couleurs */
                :root {
                    --bleu-fonce: #0F5062;
                    --bleu-clair: #7A8514;
                    --blanc: #FFFFFF;
                    --gris-clair: #E0E0E0;
                    --card-bg: #f0f4f0;
                }

                html, body {
                    height: 100%;
                    margin: 0;
                    padding: 0;
                    overflow-x: hidden;
                }

                #wrapwrap {
                    /* Supprimer l'en-tête et le pied de page standard du site */
<!--                    background-image: url('/travel_booking/static/img/wp2574287.webp');-->
                    background-size: cover;
                    background-position: center;
                    min-height: 100vh;
                    position: relative;
                }

                /* Force le contenu à occuper toute la hauteur */
                main {
                    display: flex;
                    justify-content: center;
                    align-items: center;
                    min-height: 100vh;
                }

                .welcome-container {
                    background-color: var(--card-bg);
                    border-radius: 15px;
                    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
                    width: 100%;
                    max-width: 500px;
                    padding: 40px;
                    text-align: center;
                    margin: auto; /* Centrage supplémentaire */
                }

                .logo {
                    max-width: 150px;
                    margin-bottom: 20px;
                }

                h1 {
                    color: var(--bleu-fonce);
                    font-size: 28px;
                    font-weight: 700;
                    margin-bottom: 30px;
                }

                .welcome-text {
                    color: var(--bleu-fonce);
                    font-size: 18px;
                    margin-bottom: 30px;
                    line-height: 1.6;
                }

                .btn-container {
                    display: flex;
                    flex-direction: column;
                    gap: 15px;
                    margin-top: 20px;
                }

                .btn {
                    display: block;
                    padding: 14px 20px;
                    border-radius: 8px;
                    font-weight: 600;
                    font-size: 16px;
                    text-decoration: none;
                    transition: all 0.3s ease;
                    border: none;
                    cursor: pointer;
                }

                .btn-primary {
                    background-color: var(--bleu-clair);
                    color: white;
                }

                .btn-primary:hover {
                    background-color: var(--bleu-fonce);
                    transform: translateY(-2px);
                    box-shadow: 0 4px 10px rgba(15, 80, 98, 0.3);
                }

                .btn-outline {
                    background-color: transparent;
                    border: 2px solid var(--bleu-clair);
                    color: var(--bleu-clair);
                }

                .btn-outline:hover {
                    background-color: var(--bleu-clair);
                    color: white;
                    transform: translateY(-2px);
                    box-shadow: 0 4px 10px rgba(15, 80, 98, 0.3);
                }

                .signup-text {
                    margin-top: 25px;
                    font-size: 14px;
                    color: #555;
                }

                .signup-link {
                    color: var(--bleu-clair);
                    font-weight: 600;
                    text-decoration: none;
                }

                .signup-link:hover {
                    color: var(--bleu-fonce);
                    text-decoration: underline;
                }

                /* Cacher l'en-tête et le pied de page du layout standard */
                #wrapwrap header,
                #wrapwrap footer {
                    display: none;
                }

                /* Pour garantir que la page utilise toute la hauteur disponible */
                .oe_website_sale {
                    min-height: 100vh;
                    display: flex;
                    align-items: center;
                    justify-content: center;
                }

                /* Pour forcer le conteneur de la page à prendre toute la hauteur */
                #wrap {
                    min-height: 100vh;
                    display: flex;
                    align-items: center;
                    justify-content: center;
                }
            </style>
            <div class="welcome-container">
                <img src="/travel_booking/static/img/Fly.png" class="logo" alt="Fly Now Logo"/>
                <h1>Bienvenue chez Fly Now</h1>
                <p class="welcome-text">Votre partenaire de confiance pour des voyages inoubliables partout dans le monde.</p>

                <div class="btn-container">
                    <a href="/travel/signin" class="btn btn-primary">Se connecter</a>
                    <a href="/traveling" class="btn btn-outline">Continuer comme invité</a>
                </div>

                <p class="signup-text">
                    Vous n'avez pas encore de compte ?
                    <a href="/travel/signup" class="signup-link">Inscrivez-vous</a>
                </p>
            </div>
        </t>
    </template>
</odoo>